{
  "version": 3,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/direct/(.*)", "dest": "/direct" },
    { "src": "/default/(.*)", "dest": "/default" },
    { "src": "/edge/(.*)", "dest": "/edge" }
  ]
}
